
---

## 📋 Java `String` 常用方法速查表

| 方法 | 功能说明 | 参数 | 返回值 | 示例 | 典型场景 |
|------|--------|------|--------|------|----------|
| `length()` | 获取字符串长度（字符个数） | 无 | `int` | `"Hello".length()` → `5` | 循环边界、空值判断 |
| `charAt(int index)` | 获取指定索引处的字符 | `index`：整数（≥0） | `char` | `"Java".charAt(0)` → `'J'` | 逐字符处理、校验首字母 |
| `equals(Object obj)` | 比较内容是否完全相等 | `obj`：要比较的对象 | `boolean` | `"a".equals("A")` → `false` | 安全比较字符串内容（必用！） |
| `equalsIgnoreCase(String str)` | 忽略大小写比较内容 | `str`：目标字符串 | `boolean` | `"A".equalsIgnoreCase("a")` → `true` | 用户名/命令不区分大小写 |
| `toUpperCase()` | 转为大写形式 | 无 | `String` | `"hi".toUpperCase()` → `"HI"` | 标准化输入、格式统一 |
| `toLowerCase()` | 转为小写形式 | 无 | `String` | `"HELLO".toLowerCase()` → `"hello"` | 同上 |
| `trim()` | 去除首尾空白字符（空格、`\t`、`\n`） | 无 | `String` | `"  ok  ".trim()` → `"ok"` | 清洗用户输入 |
| `substring(int begin)` | 截取从 `begin` 到末尾的子串 | `begin`：起始索引（含） | `String` | `"Hello".substring(1)` → `"ello"` | 提取后缀、去除前缀 |
| `substring(int begin, int end)` | 截取 `[begin, end)` 区间的子串 | `begin`、`end`：起止索引 | `String` | `"Hello".substring(1,4)` → `"ell"` | 提取中间部分（如域名、ID） |
| `indexOf(String str)`<br>`indexOf(char c)` | 查找首次出现位置 | 子串或字符 | `int`（未找到返回 `-1`） | `"abc".indexOf('b')` → `1` | 判断包含、定位分隔符（如 `@`） |
| `lastIndexOf(String str)` | 查找最后一次出现位置 | 子串或字符 | `int` | `"aabbcc".lastIndexOf("b")` → `3` | 从右查找（如文件扩展名） |
| `startsWith(String prefix)` | 是否以指定前缀开头 | `prefix`：前缀字符串 | `boolean` | `"https://...".startsWith("https")` → `true` | 协议校验、路径匹配 |
| `endsWith(String suffix)` | 是否以指定后缀结尾 | `suffix`：后缀字符串 | `boolean` | `"file.txt".endsWith(".txt")` → `true` | 文件类型判断 |
| `replace(char old, char new)`<br>`replace(CharSequence target, CharSequence replacement)` | 替换所有匹配项 | 旧字符/子串、新字符/子串 | `String` | `"12-34".replace('-', '/')` → `"12/34"` | 敏感词过滤、格式转换 |

> 💡 **关键原则**：
> - ✅ 所有方法均**返回新字符串**（原字符串不可变）
> - ✅ 比较内容必须用 `equals()`，**不要用 `==`**
> - ✅ `substring` 的 `endIndex` **不包含**
> - ✅ `indexOf` 未找到返回 `-1`，**不会抛异常**

> 🏷️ 标签：`#java/String` `#API速查` `#字符串处理` `#编程基础`

---

> 🌟 **一句话记忆口诀**：  
> **“长度用 `length`，取字用 `charAt`，  
> 比较用 `equals`，大小写转 `toXxx`，  
> 去空用 `trim`，截取用 `substring`，  
> 查找用 `indexOf`，前后缀用 `start/endWith`，  
> 替换用 `replace`，记得接收返回值！”**

---

