

---

# Java 二维数组（Two-Dimensional Array）

> [!info] 学习目标
> - 理解二维数组的本质：**“数组的数组”**  
> - 掌握二维数组的声明、创建、初始化和访问  
> - 能使用嵌套循环遍历二维数组  
> - 了解不规则二维数组（锯齿数组）  
> - 掌握常用操作（如打印矩阵、求和、转置）

---

## 1. 什么是二维数组？

在 Java 中，**二维数组并不是真正的“表格”**，而是**一个一维数组，其每个元素又是一个一维数组**。  
因此，二维数组是 **“数组的数组”（array of arrays）**。

### ✅ 特点：
- 每一行可以有不同的长度（支持“锯齿数组”）
- 内存中**不一定连续**
- 通过 **`[行索引][列索引]`** 访问元素

---

## 2. 声明与创建

### ✅ 语法 1：声明 + 创建（指定行列）
```java
// 声明
int[][] matrix;

// 创建 3 行 4 列的二维数组
matrix = new int[3][4];
```

### ✅ 语法 2：一步完成
```java
int[][] grid = new int[2][5]; // 2 行，每行 5 列
String[][] names = new String[3][2];
```

### ✅ 语法 3：静态初始化（推荐用于已知数据）
```java
int[][] scores = {
    {85, 90, 78},      // 第 0 行
    {92, 88, 95},      // 第 1 行
    {76, 82, 89}       // 第 2 行
};
```

> ⚠️ 注意：每行用 `{}` 包裹，整体再用一层 `{}`

---

## 3. 访问元素

使用 **`array[row][col]`** 语法：

```java
int[][] mat = {{1, 2}, {3, 4}};
System.out.println(mat[0][1]); // 输出 2
mat[1][0] = 99;                // 修改为 99
```

### ⚠️ 常见错误：
- 索引越界：`mat[2][0]`（只有 0~1 行）
- 对未初始化的行访问列：见“锯齿数组”部分

---

## 4. 获取行数与列数

- **行数**：`array.length`
- **某一行的列数**：`array[i].length`

```java
int[][] arr = {{1, 2, 3}, {4, 5}};
System.out.println("行数: " + arr.length);        // 2
System.out.println("第0行列数: " + arr[0].length); // 3
System.out.println("第1行列数: " + arr[1].length); // 2
```

> 💡 这说明：**每一行可以有不同长度！**

---

## 5. 遍历二维数组

### ✅ 方法 1：嵌套 `for` 循环（最常用）
```java
for (int i = 0; i < matrix.length; i++) {
    for (int j = 0; j < matrix[i].length; j++) {
        System.out.print(matrix[i][j] + " ");
    }
    System.out.println(); // 换行
}
```

### ✅ 方法 2：增强型 `for` 循环（for-each）
```java
for (int[] row : matrix) {
    for (int value : row) {
        System.out.print(value + " ");
    }
    System.out.println();
}
```

> ✅ 优点：代码简洁，自动处理行长度差异  
> ❌ 缺点：无法直接获取行/列索引

---

## 6. 不规则二维数组（锯齿数组 / Jagged Array）

Java 允许每行长度不同，称为 **锯齿数组（Jagged Array）**：

```java
int[][] jagged = new int[3][]; // 只指定行数，列数待定

jagged[0] = new int[2]; // 第0行有2列
jagged[1] = new int[4]; // 第1行有4列
jagged[2] = new int[1]; // 第2行有1列

// 或直接初始化
int[][] irregular = {
    {1},
    {2, 3},
    {4, 5, 6, 7}
};
```

> 💡 应用场景：表示树形结构、不规则表格等

---

## 7. 常用操作示例

### 🔹 1. 打印整个二维数组
```java
import java.util.Arrays;

int[][] mat = {{1,2},{3,4}};
System.out.println(Arrays.deepToString(mat)); // [[1, 2], [3, 4]]
```

### 🔹 2. 求所有元素之和
```java
int sum = 0;
for (int[] row : matrix) {
    for (int val : row) {
        sum += val;
    }
}
```

### 🔹 3. 矩阵转置（行列互换）
```java
int[][] original = {{1, 2, 3}, {4, 5, 6}}; // 2x3
int[][] transposed = new int[3][2];        // 3x2

for (int i = 0; i < original.length; i++) {
    for (int j = 0; j < original[i].length; j++) {
        transposed[j][i] = original[i][j];
    }
}
```

### 🔹 4. 查找最大值
```java
int max = matrix[0][0];
for (int[] row : matrix) {
    for (int val : row) {
        if (val > max) max = val;
    }
}
```

---

## 8. 二维数组作为方法参数

```java
public static void printMatrix(int[][] mat) {
    for (int[] row : mat) {
        System.out.println(Arrays.toString(row));
    }
}

// 调用
int[][] data = {{1,2},{3,4}};
printMatrix(data);
```

> 💡 注意：传递的是**引用**，方法内修改会影响原数组

---

## 9. ⚠️ 常见错误与注意事项

| 错误 | 说明 | 修正 |
|------|------|------|
| `mat.length()` | 误用方法 | 用 `.length`（属性） |
| 假设所有行列数相同 | 忽略锯齿数组可能 | 始终用 `mat[i].length` 获取列数 |
| 直接打印二维数组 | `System.out.println(mat)` → 地址 | 用 `Arrays.deepToString()` |
| 初始化不完整 | `new int[3][]` 后未初始化每行 | 必须为每行 `new int[n]` |

---

## 10. 小练习

以下代码输出什么？
```java
int[][] arr = new int[2][];
arr[0] = new int[]{1, 2};
arr[1] = new int[]{3};
System.out.println(arr[0].length + arr[1].length);
```

<details>
<summary>✅ 答案（点击展开）</summary>

输出：**3**

> 💡 解析：第0行长度2，第1行长度1，总和为3。
</details>

---

