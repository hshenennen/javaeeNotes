

---

# Java 数组（Array）

> [!info] 学习目标
> - 理解数组的概念与特点  
> - 掌握一维数组的声明、创建、初始化和访问  
> - 学会使用循环遍历数组  
> - 了解数组的常见操作（求和、查找、排序）  
> - 避免常见错误（如索引越界）

---

## 1. 什么是数组？

**数组（Array）** 是一种**引用数据类型**，用于**存储多个相同类型的数据**，这些数据在内存中**连续存放**，并通过**索引（index）** 访问。

### ✅ 数组的核心特点：
- **长度固定**：创建后不能改变大小
- **元素类型相同**：所有元素必须是同一类型（如 `int[]` 只能存整数）
- **索引从 0 开始**：第一个元素索引为 `0`，最后一个为 `length - 1`
- **属于对象**：数组本身是 `Object` 的子类

---

## 2. 声明与创建数组

### ✅ 语法 1：先声明，再创建
```java
// 声明
int[] numbers;        // 推荐写法（类型贴近变量名）
// 或
int numbers[];

// 创建（分配内存）
numbers = new int[5]; // 创建长度为 5 的整型数组
```

### ✅ 语法 2：声明 + 创建 一步完成
```java
int[] scores = new int[10]; // 长度为 10 的整型数组
String[] names = new String[3]; // 长度为 3 的字符串数组
```

### ✅ 语法 3：声明 + 初始化（静态初始化）
```java
int[] ages = {25, 30, 35, 40}; // 自动推断长度为 4
String[] colors = {"红", "绿", "蓝"};
```

> ⚠️ 注意：`{...}` 初始化**只能在声明时使用**，不能用于后续赋值：
> ```java
> int[] arr;
> arr = {1, 2, 3}; // ❌ 编译错误！
> arr = new int[]{1, 2, 3}; // ✅ 正确
> ```

---

## 3. 访问数组元素

通过 **索引（index）** 访问或修改元素：

```java
int[] nums = {10, 20, 30, 40};

System.out.println(nums[0]); // 输出 10
nums[1] = 25;                // 修改第二个元素
System.out.println(nums[1]); // 输出 25
```

### ⚠️ 常见错误：**数组索引越界**
```java
int[] arr = new int[3]; // 索引：0, 1, 2
System.out.println(arr[3]); // ❌ ArrayIndexOutOfBoundsException
```

---

## 4. 数组的长度

使用 `.length` 属性获取数组长度（**不是方法，没有括号！**）：

```java
int[] data = {1, 2, 3, 4, 5};
System.out.println("数组长度: " + data.length); // 输出 5
```

> 💡 应用：结合 `for` 循环安全遍历
```java
for (int i = 0; i < data.length; i++) {
    System.out.println(data[i]);
}
```

---

## 5. 遍历数组的三种方式

### ✅ 1. 普通 `for` 循环（带索引）
```java
for (int i = 0; i < arr.length; i++) {
    System.out.println("arr[" + i + "] = " + arr[i]);
}
```

### ✅ 2. 增强型 `for` 循环（for-each，推荐只读）
```java
for (int value : arr) {
    System.out.println(value);
}
```
> ⚠️ 限制：**不能修改原数组元素**（`value` 是副本）

### ✅ 3. 使用 `Arrays.toString()` 打印整个数组
```java
import java.util.Arrays;
int[] arr = {1, 2, 3};
System.out.println(Arrays.toString(arr)); // [1, 2, 3]
```

---

## 6. 常见数组操作

### 🔹 1. 求数组元素总和
```java
int sum = 0;
for (int num : numbers) {
    sum += num;
}
```

### 🔹 2. 查找最大值
```java
int max = numbers[0];
for (int i = 1; i < numbers.length; i++) {
    if (numbers[i] > max) {
        max = numbers[i];
    }
}
```

### 🔹 3. 数组排序（使用 `Arrays.sort()`）
```java
import java.util.Arrays;
int[] arr = {5, 2, 8, 1};
Arrays.sort(arr); // 升序排序
System.out.println(Arrays.toString(arr)); // [1, 2, 5, 8]
```

### 🔹 4. 查找元素是否存在
```java
boolean found = false;
for (int num : arr) {
    if (num == target) {
        found = true;
        break;
    }
}
```

---

## 7. 数组作为方法参数和返回值

### ✅ 传递数组给方法（引用传递）
```java
public static void printArray(int[] arr) {
    for (int x : arr) {
        System.out.print(x + " ");
    }
}

// 调用
int[] data = {1, 2, 3};
printArray(data); // 输出: 1 2 3
```

> 💡 注意：方法内修改数组元素，**会影响原数组**（因为传的是引用）

### ✅ 方法返回数组
```java
public static int[] createArray(int size) {
    return new int[size];
}
```

---

## 8. 多维数组（了解）

Java 支持多维数组，最常见的是**二维数组**（可理解为“数组的数组”）：

```java
// 创建 3x4 的二维数组
int[][] matrix = new int[3][4];

// 初始化
int[][] grid = {
    {1, 2, 3},
    {4, 5, 6},
    {7, 8, 9}
};

// 访问元素
System.out.println(grid[1][2]); // 输出 6

// 遍历
for (int i = 0; i < grid.length; i++) {
    for (int j = 0; j < grid[i].length; j++) {
        System.out.print(grid[i][j] + " ");
    }
    System.out.println();
}
```

---

## 9. 常见错误与最佳实践

> [!danger] 初学者易错点

| 错误 | 说明 | 修正 |
|------|------|------|
| `length()` | 误用方法调用 | 用 `.length`（属性） |
| 索引越界 | 访问 `arr[arr.length]` | 索引范围：`0 ~ length-1` |
| 忘记初始化 | `int[] arr; System.out.println(arr[0]);` | 先 `new` 或 `{}` 初始化 |
| 直接打印数组 | `System.out.println(arr);` | 用 `Arrays.toString(arr)` |

> ✅ **最佳实践**：
> - 优先使用增强型 `for` 循环遍历（简洁安全）
> - 需要修改元素或使用索引时，用普通 `for`
> - 复杂数据结构考虑使用 `ArrayList`（后续学习）

---

## 10. 小练习

写出以下代码的输出：

```java
int[] a = {1, 2, 3};
int[] b = a;
b[0] = 99;
System.out.println(a[0]);
```

<details>
<summary>✅ 答案</summary>

输出：**99**

> 💡 原因：`b = a` 是**引用赋值**，`a` 和 `b` 指向同一数组，修改 `b[0]` 会影响 `a[0]`。
</details>

---


