

---

# Java 变量（Variables）

> [!info] 学习目标
> - 理解什么是变量
> - 掌握变量的声明、初始化和命名规则
> - 区分局部变量、成员变量和类变量（static）
> - 了解变量的作用域与生命周期

---

## 1. 什么是变量？

- **变量** 是用于**存储数据的容器**。
- 每个变量都有：
  - **名称（标识符）**
  - **数据类型**（如 `int`, `double`, `boolean`）
  - **值**（可变）
  - **作用域**（在哪些地方能使用）

> 💡 类比：变量就像贴了标签的盒子，标签是变量名，盒子里装的是值。

---

## 2. 声明与初始化变量

### 基本语法
```java
数据类型 变量名 = 初始值;
```

### 示例
```java
int age = 20;                // 整数
double price = 19.99;        // 小数
char grade = 'A';            // 字符
boolean isActive = true;     // 布尔值
String name = "Alice";       // 字符串（引用类型）
```

> ✅ 你也可以先声明，后赋值：
```java
int score;
score = 100;
```

> ⚠️ **局部变量必须初始化后才能使用**，否则编译报错！

---

## 3. 变量命名规则

### ✅ 合法命名（必须遵守）
- 只能包含：字母、数字、`$`、`_`
- 不能以数字开头
- 不能是 Java 关键字（如 `int`, `class`, `public`）
- 区分大小写（`age` 和 `Age` 是两个变量）

### 📌 推荐命名规范（约定俗成）
- **驼峰命名法（camelCase）**：首单词小写，后续单词首字母大写  
  例如：`studentName`, `totalPrice`, `isReady`
- 常量（`final` 变量）用**全大写 + 下划线**：`MAX_SPEED`, `PI`

### ❌ 错误示例
```java
int 1number;   // 不能以数字开头
int my-name;   // 不能用连字符
int class;     // class 是关键字
```

---

## 4. 变量的三种类型

| 类型 | 说明 | 声明位置 | 是否有默认值 | 生命周期 |
|------|------|----------|---------------|----------|
| **局部变量** | 定义在方法、构造器或代码块中 | 方法内部 | ❌ 没有（必须手动初始化） | 方法调用开始 → 方法结束 |
| **成员变量（实例变量）** | 定义在类中，方法外 | 类内、方法外 | ✅ 有（如 `int` 默认为 `0`） | 对象创建 → 对象被回收 |
| **类变量（静态变量）** | 用 `static` 修饰的成员变量 | 类内、方法外，加 `static` | ✅ 有 | 类加载 → 程序结束 |

### 示例对比
```java
public class Student {
    // 成员变量（实例变量）
    String name;        // 默认 null
    int age;            // 默认 0

    // 类变量（静态变量）
    static String school = "光明中学";

    public void study() {
        // 局部变量
        int hours = 2;  // 必须初始化！
        System.out.println(name + " 学习了 " + hours + " 小时");
    }
}
```

使用：
```java
Student s1 = new Student();
s1.name = "小明";
s1.study(); // 输出：小明 学习了 2 小时

System.out.println(Student.school); // 光明中学（通过类名访问 static 变量）
```

---

## 5. 变量的作用域（Scope）

- **作用域**：变量可以被访问的代码范围。
- Java 中作用域由 `{}` 决定。

### 示例
```java
public class ScopeDemo {
    public static void main(String[] args) {
        int x = 10; // x 的作用域：整个 main 方法

        if (true) {
            int y = 20; // y 的作用域：仅限这个 if 块
            System.out.println(x + y); // ✅ 可以访问 x 和 y
        }

        // System.out.println(y); // ❌ 编译错误！y 已超出作用域
    }
}
```

---

## 6. final 变量（常量）

- 用 `final` 修饰的变量**只能赋值一次**，之后不可更改。
- 通常用于定义常量。

```java
final double PI = 3.14159;
// PI = 3.14; // ❌ 编译错误！

final String APP_NAME = "MyJavaApp"; // 常量命名习惯：全大写
```

> 💡 `final` 修饰引用类型时，**引用不能变，但对象内容可以变**（进阶内容）。

---

## 7. 小练习

尝试写出以下程序的输出：

```java
public class VariableTest {
    static int count = 0; // 类变量

    public static void main(String[] args) {
        int a = 5;
        {
            int b = 10;
            System.out.println(a + b); // ?
        }
        // System.out.println(b); // 这行取消注释会怎样？
        System.out.println(count); // ?
    }
}
```

<details>
<summary>✅ 答案</summary>

- 输出：`15` 和 `0`
- 如果取消注释 `System.out.println(b);`，会**编译错误**，因为 `b` 超出作用域。
</details>
    
---

## 8. 常见误区提醒

> [!warning] 初学者注意
> - 局部变量**不会自动初始化**！必须赋值后才能使用。
> - `String` 是引用类型，不是基本类型（但它很常用，容易混淆）。
> - `static` 变量属于**类**，不属于某个对象，所有对象共享同一个值。

