

---

# Java 中的打印（输出）语句

> [!info] 学习目标
> - 掌握 `System.out.print()` 与 `System.out.println()` 的区别  
> - 学会使用 `System.out.printf()` 进行格式化输出  
> - 理解如何打印变量、表达式和对象  
> - 避免常见打印错误（如换行问题、格式符不匹配）

---

## 1. 最常用的两种打印方式

### ✅ 1. `System.out.println()`
- **打印内容 + 自动换行**
- 最常用，适合每行输出一个结果

```java
System.out.println("Hello, World!");
System.out.println(42);
System.out.println(true);
```

**输出**：
```
Hello, World!
42
true
```

---

### ✅ 2. `System.out.print()`
- **打印内容，但不换行**
- 适合在同一行连续输出多个内容

```java
System.out.print("A");
System.out.print("B");
System.out.print("C");
```

**输出**：
```
ABC
```

> 💡 对比：`println` = `print` + `\n`（换行符）

---

## 2. 打印变量与表达式

Java 会自动将基本类型、字符串、对象转换为字符串形式输出。

### 🌰 示例
```java
int age = 25;
double price = 19.99;
String name = "Alice";

System.out.println("姓名: " + name);           // 字符串拼接
System.out.println("年龄: " + age);            // int 自动转字符串
System.out.println("价格: " + price);          // double 自动转字符串
System.out.println("明年年龄: " + (age + 1));  // 先计算表达式，再拼接
```

**输出**：
```
姓名: Alice
年龄: 25
价格: 19.99
明年年龄: 26
```

> ⚠️ 注意运算符优先级：`"年龄: " + age + 1` → `"年龄: 251"`（字符串拼接），  
> 要先计算请加括号：`"年龄: " + (age + 1)`

---

## 3. 格式化输出：`System.out.printf()`

类似 C 语言的 `printf`，用于**控制输出格式**（如保留小数、对齐、补零等）。

### 基本语法：
```java
System.out.printf("格式字符串", 参数1, 参数2, ...);
```

### 常用格式符：
| 格式符 | 说明 | 示例 |
|--------|------|------|
| `%d` | 十进制整数 | `printf("%d", 100)` → `100` |
| `%f` | 浮点数 | `printf("%.2f", 3.1415)` → `3.14` |
| `%s` | 字符串 | `printf("%s", "Hi")` → `Hi` |
| `%c` | 字符 | `printf("%c", 'A')` → `A` |
| `%%` | 输出 `%` 符号 | `printf("100%%")` → `100%` |

### 🌰 示例：控制小数位数与对齐
```java
double pi = 3.1415926;
int score = 95;

System.out.printf("圆周率: %.2f\n", pi);        // 保留2位小数
System.out.printf("成绩: %5d 分\n", score);     // 右对齐，占5字符宽
System.out.printf("姓名: %-10s\n", "张三");     // 左对齐，占10字符宽
```

**输出**：
```
圆周率: 3.14
成绩:    95 分
姓名: 张三      
```

> 💡 `%5d` 表示至少占 5 个字符宽度，不足补空格（右对齐）  
> `%-10s` 表示左对齐，占 10 个字符

---

## 4. 打印数组和对象

### ❌ 直接打印数组会显示内存地址（不推荐）：
```java
int[] arr = {1, 2, 3};
System.out.println(arr); // 输出类似 [I@1b6d3586
```

### ✅ 正确方式：使用 `Arrays.toString()`
```java
import java.util.Arrays;

int[] arr = {1, 2, 3};
System.out.println(Arrays.toString(arr)); // [1, 2, 3]
```

### 对象打印：
- 默认调用 `toString()` 方法
- 建议在自定义类中**重写 `toString()`** 以获得有意义的输出

```java
class Person {
    String name;
    Person(String name) { this.name = name; }
    @Override
    public String toString() {
        return "Person{name='" + name + "'}";
    }
}

Person p = new Person("Bob");
System.out.println(p); // Person{name='Bob'}
```

---

## 5. 换行与特殊字符

| 转义符 | 作用 |
|--------|------|
| `\n` | 换行（newline） |
| `\t` | 制表符（tab，对齐用） |
| `\"` | 输出双引号 |
| `\\` | 输出反斜杠 |

### 🌰 示例
```java
System.out.print("第一行\n第二行\t对齐\n");
System.out.println("他说：\"你好！\"");
```

**输出**：
```
第一行
第二行	对齐
他说："你好！"
```

---

## 6. 常见错误与最佳实践

> [!danger] 初学者易错点

| 错误 | 说明 | 修正 |
|------|------|------|
| 忘记引号 | `println(Hello)` | `println("Hello")` |
| 拼接时优先级错误 | `"结果: " + a + b` 当 `a=1, b=2` → `"结果: 12"` | 用括号：`"结果: " + (a + b)` |
| `printf` 参数不匹配 | `printf("%d", 3.14)` | 类型要匹配：`%f` 对应浮点数 |
| 直接打印数组 | 输出无意义地址 | 用 `Arrays.toString(arr)` |

> ✅ **最佳实践**：
> - 调试时多用 `println` 快速查看变量值
> - 正式输出建议用 `printf` 控制格式
> - 自定义类务必重写 `toString()`

---

## 7. 小练习

写出以下代码的输出：

```java
int x = 10, y = 3;
System.out.print("x=" + x + ", y=" + y + "\n");
System.out.printf("x/y = %.1f\n", (double)x / y);
System.out.println("结束");
```

<details>
<summary>✅ 答案</summary>

```
x=10, y=3
x/y = 3.3
结束
```
</details>

---



