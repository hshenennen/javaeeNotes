
---

# Java 中的 `if` 语句（条件判断）

> [!info] 学习目标
> - 掌握 `if`、`if-else`、`if-else if-else` 的语法
> - 理解条件表达式的布尔本质
> - 能编写简单的分支逻辑程序
> - 避免常见语法错误（如缺少大括号、误用赋值）

---

## 1. 什么是 `if` 语句？

`if` 是 Java 中用于**条件判断**的控制语句。  
它根据**布尔表达式（true/false）** 决定是否执行某段代码。

> 💡 核心思想：**“如果……就……”**

---

## 2. 基本语法

### ✅ 1. 简单 `if`
```java
if (条件表达式) {
    // 条件为 true 时执行的代码
}
```

### ✅ 2. `if-else`
```java
if (条件表达式) {
    // 条件为 true 时执行
} else {
    // 条件为 false 时执行
}
```

### ✅ 3. `if-else if-else`（多分支）
```java
if (条件1) {
    // 执行 A
} else if (条件2) {
    // 执行 B
} else if (条件3) {
    // 执行 C
} else {
    // 所有条件都不满足时执行
}
```

> 🔁 **注意**：一旦某个 `if` 或 `else if` 条件为 `true`，**后续所有分支都会被跳过**。

---

## 3. 条件表达式要求

- **必须是 `boolean` 类型**（`true` 或 `false`）
- 通常由**比较运算符**（`>`, `==`, `!=`）或**逻辑运算符**（`&&`, `||`）构成

### 🌰 合法示例
```java
int age = 18;
if (age >= 18) {
    System.out.println("成年人");
}

boolean isLoggedIn = true;
if (isLoggedIn) { // 直接使用 boolean 变量
    System.out.println("欢迎回来！");
}
```

### ❌ 非法示例
```java
int x = 5;
if (x) { ... }        // ❌ 编译错误！x 是 int，不是 boolean
if (x = 10) { ... }   // ❌ 编译错误！= 是赋值，不是比较（应为 ==）
```

---

## 4. 大括号 `{}` 的使用建议

- 如果 `if` 后只有一行代码，**可以省略大括号**：
  ```java
  if (score > 60)
      System.out.println("及格");
  ```
- **但强烈建议始终使用大括号**，避免以下陷阱：

### ⚠️ 危险示例（无大括号）
```java
if (isAdmin)
    System.out.println("权限高");
    deleteAllUsers(); // ❌ 这行**总会执行**！不属于 if 块
```

> ✅ 正确写法：
```java
if (isAdmin) {
    System.out.println("权限高");
    deleteAllUsers(); // 只有管理员才执行
}
```

---

## 5. 嵌套 `if`（if inside if）

可以在 `if` 块中再写 `if`，实现更复杂的逻辑。

```java
int age = 20;
boolean hasLicense = true;

if (age >= 18) {
    if (hasLicense) {
        System.out.println("可以开车");
    } else {
        System.out.println("需要考驾照");
    }
} else {
    System.out.println("未成年不能开车");
}
```

> 💡 嵌套不宜过深，否则影响可读性（可考虑用逻辑运算符合并条件）

---

## 6. 常见应用场景

| 场景 | 示例 |
|------|------|
| 成绩判断 | `if (score >= 90) grade = "A";` |
| 登录验证 | `if (username.equals("admin") && password.equals("123"))` |
| 数值范围 | `if (temperature > 0 && temperature < 100)` |
| 奇偶判断 | `if (num % 2 == 0) System.out.println("偶数");` |

---

## 7. 小练习

写出以下代码的输出：

```java
int x = 10;
if (x > 5) {
    if (x < 15) {
        System.out.println("A");
    } else {
        System.out.println("B");
    }
} else {
    System.out.println("C");
}
```

<details>
<summary>✅ 答案（）</summary>

- `x = 10`，满足 `x > 5` → 进入第一个 if
- `x < 15` 为 true → 输出 **A**
</details>

---

## 8. 常见错误总结

> [!danger] 初学者易错点

| 错误 | 说明 | 修正 |
|------|------|------|
| `if (a = 5)` | 用了赋值 `=` 而不是比较 `==` | `if (a == 5)` |
| 忘记大括号导致逻辑错误 | 多行代码未包裹 | 始终用 `{}` |
| 对 `String` 用 `==` 比较 | 比较的是地址，不是内容 | 用 `.equals()` |
| 条件写成非 boolean | `if (number)` | `if (number != 0)` 或 `if (flag)` |

---

## 9. 与 `switch` 的区别（提前了解）

- `if`：适合**区间判断**、**复杂条件**（如 `x > 10 && y < 5`）
- `switch`：适合**等值判断**（如菜单选项、状态码）

---


