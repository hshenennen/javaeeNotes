`StringBuffer` 是 Java 中一个**可变的、线程安全的字符串类**，用于高效地操作（拼接、修改、插入等）字符序列。

它和 `StringBuilder` 非常相似，但有一个关键区别：**`StringBuffer` 是线程安全的（thread-safe）**，而 `StringBuilder` 不是。

---

### 🔹 1. **基本概念**

- `StringBuffer` 表示一个**可变的字符序列**。
- 内部使用一个动态扩容的 `char[]` 数组存储数据。
- 所有修改方法（如 `append()`、`insert()`、`reverse()` 等）都是 **`synchronized` 的**，即加了锁，保证多线程环境下操作安全。

---

### 🔹 2. **为什么需要 StringBuffer？**

回顾一下 `String` 的问题：
```java
String s = "a";
s += "b"; // 实际创建了一个新对象！
```
频繁拼接会产生大量临时对象，效率低。

为了解决这个问题，Java 提供了两个可变字符串类：
- `StringBuffer`（JDK 1.0 就有了）
- `StringBuilder`（JDK 1.5 新增）

> 💡 `StringBuilder` 其实就是 `StringBuffer` 的“非线程安全版”，性能更高。

---

### 🔹 3. **常用方法（和 StringBuilder 几乎一样）**

```java
StringBuffer sb = new StringBuffer();

sb.append("Hello");        // 追加
sb.insert(5, " World");    // 在索引5处插入
sb.reverse();              // 反转
sb.delete(0, 5);           // 删除 [0,5) 的字符
sb.replace(0, 5, "Hi");    // 替换 [0,5) 为 "Hi"
String result = sb.toString(); // 转成 String
```

这些方法都返回 `StringBuffer` 本身（支持链式调用）。

---

### 🔹 4. **线程安全是怎么实现的？**

`StringBuffer` 的关键方法都加了 `synchronized` 关键字：

```java
public synchronized StringBuffer append(String str) {
    // ... 实现
    return this;
}
```

这意味着：
- 多个线程同时操作同一个 `StringBuffer` 对象时，**不会出现数据错乱**。
- 但每次操作都要获取对象锁，**性能比 `StringBuilder` 低**。

---

### 🔹 5. **StringBuffer vs StringBuilder vs String**

| 特性 | `String` | `StringBuilder` | `StringBuffer` |
|------|--------|------------------|----------------|
| 可变性 | ❌ 不可变 | ✅ 可变 | ✅ 可变 |
| 线程安全 | ✅（天然） | ❌ 不安全 | ✅ 安全 |
| 性能 | 低（频繁修改时） | ⭐ 最高 | 中等（因同步开销） |
| 适用场景 | 字符串常量、只读 | 单线程字符串构建 | 多线程共享字符串缓冲区 |

---

### 🔹 6. **什么时候用 StringBuffer？**

✅ **极少数情况**需要它，比如：

- 你在一个**多线程环境**中，多个线程要**同时修改同一个字符串缓冲区**。
- 且你无法通过其他方式（如局部变量、锁控制）避免并发冲突。

❌ **绝大多数情况不需要**！  
现代 Java 开发中，字符串构建几乎都在**单线程上下文**（如方法内部），直接用 `StringBuilder` 更高效。

> 📌 官方建议：除非明确需要线程安全，否则优先使用 `StringBuilder`。

---

### 🔹 7. **简单示例**

```java
public class Example {
    public static void main(String[] args) {
        StringBuffer buffer = new StringBuffer("Java");
        buffer.append(" is");
        buffer.append(" fun!");
        System.out.println(buffer.toString()); // 输出: Java is fun!

        buffer.reverse();
        System.out.println(buffer); // 输出: !nuf si avaJ
    }
}
```

---

### ✅ 总结

- `StringBuffer` = 可变 + 线程安全 + 性能较低。
- 它是早期 Java 为了解决 `String` 拼接效率问题而设计的。
- 如今，**`StringBuilder` 是更主流的选择**。
- 除非你在写多线程共享的字符串缓冲逻辑（非常少见），否则不用它。

如果你是在学习或做练习题，**掌握 `StringBuilder` 就足够了**，知道 `StringBuffer` 的存在和区别即可 😊

