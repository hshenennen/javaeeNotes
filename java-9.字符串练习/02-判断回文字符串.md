

---

## 📌 题目：判断回文字符串（Palindrome）

**题目描述**：  
编写一个 Java 程序，判断一个字符串是否为回文。  
> 回文：正着读和反着读都一样的字符串，例如 `"level"`、`"madam"`、`"A man a plan a canal Panama"`（忽略大小写和非字母数字字符时）。

---

## 💡 思路分析

判断回文的核心思想是：**字符串与其反转后的字符串是否相等**。  
但也可以通过**双指针法**（从两端向中间比较）来实现，更高效。

常见变体：
- **严格回文**：区分大小写，包含所有字符（如 `"Level"` 不是回文）
- **宽松回文**：忽略大小写、空格、标点（如 `"A man, a plan, a canal: Panama"` 是回文）

作为初学者，建议先实现**严格回文**，再尝试**宽松回文**。

---

## ✅ 方法一：使用 `StringBuilder` 反转比较（严格回文）

```java
public class PalindromeCheck {
    public static boolean isPalindrome(String s) {
        String reversed = new StringBuilder(s).reverse().toString();
        return s.equals(reversed);
    }

    public static void main(String[] args) {
        String test = "level";
        System.out.println(test + " 是回文吗？" + isPalindrome(test)); // true
    }
}
```

> ✅ 简单直观，适合初学者  
> ⚠️ 缺点：需要额外空间存储反转字符串

---

## ✅ 方法二：双指针法（推荐，更高效）

```java
public class PalindromeCheckTwoPointers {
    public static boolean isPalindrome(String s) {
        int left = 0;
        int right = s.length() - 1;
        
        while (left < right) {
            if (s.charAt(left) != s.charAt(right)) {
                return false;
            }
            left++;
            right--;
        }
        return true;
    }

    public static void main(String[] args) {
        System.out.println(isPalindrome("madam")); // true
        System.out.println(isPalindrome("hello")); // false
    }
}
```

> ✅ 时间复杂度 O(n/2)，空间复杂度 O(1)  
> ✅ 不需要创建新字符串，效率更高

---

## ✅ 方法三：宽松回文（忽略大小写、非字母数字字符）

```java
public class PalindromeIgnoreCase {
    public static boolean isPalindrome(String s) {
        // 只保留字母和数字，并转为小写
        StringBuilder clean = new StringBuilder();
        for (char c : s.toCharArray()) {
            if (Character.isLetterOrDigit(c)) {
                clean.append(Character.toLowerCase(c));
            }
        }
        String cleanedStr = clean.toString();
        
        // 双指针判断
        int left = 0, right = cleanedStr.length() - 1;
        while (left < right) {
            if (cleanedStr.charAt(left) != cleanedStr.charAt(right)) {
                return false;
            }
            left++;
            right--;
        }
        return true;
    }

    public static void main(String[] args) {
        System.out.println(isPalindrome("A man, a plan, a canal: Panama")); // true
        System.out.println(isPalindrome("race a car")); // false
    }
}
```

> 🔑 关键方法：
> - `Character.isLetterOrDigit(c)`：判断是否为字母或数字
> - `Character.toLowerCase(c)`：转小写

---

## 📚 知识点总结

| 概念 | 说明 |
|------|------|
| `StringBuilder.reverse()` | 快速反转字符串 |
| `charAt(index)` | 获取指定位置字符 |
| 双指针技巧 | 从两端向中间遍历，常用于回文、对称问题 |
| `Character.isLetterOrDigit()` | 判断字符是否为字母或数字 |
| `String.equals()` | 比较字符串内容（不是 `==`！） |

---

## 🧠 小练习建议

1. 写一个方法，判断用户输入的单词是否为回文（使用 `Scanner`）。
2. 扩展程序，支持忽略大小写（提示：先用 `toLowerCase()`）。
3. 尝试不用 `StringBuilder`，仅用循环实现反转并比较。
4. （挑战）判断一个整数是否为回文数（不转成字符串）。

---

