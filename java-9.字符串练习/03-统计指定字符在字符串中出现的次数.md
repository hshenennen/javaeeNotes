

---

## 📌 题目：统计指定字符在字符串中出现的次数

**题目描述**：  
给定一个字符串 `str` 和一个字符 `target`，返回 `target` 在 `str` 中出现的次数。

**示例**：  
- 输入：`str = "programming"`, `target = 'm'`  
- 输出：`2`

---

## 💡 思路分析

1. 遍历字符串中的每一个字符。
2. 如果当前字符等于目标字符，计数器加 1。
3. 遍历结束后，返回计数器的值。

> ✅ 不需要使用 `Map`（因为只关心一个字符）  
> ✅ 时间复杂度：O(n)，空间复杂度：O(1)

---

## ✅ 方法一：使用 `for-each` 循环（推荐）

```java
public class CountCharOccurrences {
    public static int countChar(String str, char target) {
        int count = 0;
        for (char c : str.toCharArray()) {
            if (c == target) {
                count++;
            }
        }
        return count;
    }

    public static void main(String[] args) {
        String text = "programming";
        char ch = 'm';
        int result = countChar(text, ch);
        System.out.println("字符 '" + ch + "' 在 \"" + text + "\" 中出现了 " + result + " 次。");
        // 输出：字符 'm' 在 "programming" 中出现了 2 次。
    }
}
```

---

## ✅ 方法二：使用传统 `for` 循环（等效）

```java
public static int countChar(String str, char target) {
    int count = 0;
    for (int i = 0; i < str.length(); i++) {
        if (str.charAt(i) == target) {
            count++;
        }
    }
    return count;
}
```

> 两种方法效率几乎相同，`for-each` 更简洁易读。

---

## ✅ 方法三：忽略大小写（扩展功能）

如果希望 `'A'` 和 `'a'` 被视为相同字符：

```java
public static int countCharIgnoreCase(String str, char target) {
    str = str.toLowerCase();
    target = Character.toLowerCase(target);
    return countChar(str, target); // 复用上面的方法
}
```

---

## 📚 知识点总结

| 概念 | 说明 |
|------|------|
| `toCharArray()` | 将字符串转为字符数组，用于 `for-each` 遍历 |
| `charAt(int index)` | 获取指定位置的字符 |
| `char` 比较 | 使用 `==`（因为 `char` 是基本类型） |
| 方法复用 | 将核心逻辑封装成方法，便于扩展（如忽略大小写） |

---

## 🧠 小练习建议

1. 编写一个带用户输入的版本（使用 `Scanner`）：
   ```java
   Scanner sc = new Scanner(System.in);
   System.out.print("请输入字符串: ");
   String s = sc.nextLine();
   System.out.print("请输入要统计的字符: ");
   char c = sc.next().charAt(0);
   ```
2. 测试边界情况：
   - 空字符串 `""`
   - 字符串中不含目标字符
   - 目标字符是空格 `' '`
3. 尝试用递归实现（挑战）：
   ```java
   // 提示：递归终止条件是字符串为空
   ```

---

