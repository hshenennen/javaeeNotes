
---

## 📌 题目：字符串中所有连续数字的和

**题目描述**：  
给定一个包含字母、数字和其他字符的字符串，提取其中**所有连续的数字子串**（如 `"12"`, `"3"`, `"45"`），将它们转换为整数并求和。

**示例**：
- 输入：`"a12b3c45"`  
- 提取：`12`, `3`, `45`  
- 输出：`12 + 3 + 45 = 60`

---

## 💡 思路分析

1. **遍历字符串的每个字符**
2. **遇到数字字符时，开始“收集”连续的数字**
3. **当遇到非数字字符（或字符串结束）时，将已收集的数字字符串转为整数并累加**
4. **重置数字缓冲区，继续后续扫描**

> 🔑 关键点：如何识别“连续数字的开始和结束”？

推荐使用一个 `StringBuilder` 或字符串变量临时存储当前数字片段。

---

## ✅ 方法一：使用 `StringBuilder` 缓冲数字（推荐）

```java
public class SumOfNumbersInString {
    public static int sumNumbers(String str) {
        StringBuilder numBuffer = new StringBuilder();
        int total = 0;

        for (int i = 0; i < str.length(); i++) {
            char c = str.charAt(i);
            if (Character.isDigit(c)) {
                // 是数字，加入缓冲区
                numBuffer.append(c);
            } else {
                // 遇到非数字，处理缓冲区中的数字
                if (numBuffer.length() > 0) {
                    total += Integer.parseInt(numBuffer.toString());
                    numBuffer.setLength(0); // 清空缓冲区
                }
            }
        }

        // 字符串结尾可能是数字（如 "abc123"），需再检查一次
        if (numBuffer.length() > 0) {
            total += Integer.parseInt(numBuffer.toString());
        }

        return total;
    }

    public static void main(String[] args) {
        String input = "a12b3c45";
        int result = sumNumbers(input);
        System.out.println("字符串 \"" + input + "\" 中数字之和为: " + result); // 60
    }
}
```

---

## ✅ 方法二：不使用 `StringBuilder`，用字符串拼接（效率较低，仅演示）

```java
public static int sumNumbers(String str) {
    String currentNum = "";
    int total = 0;

    for (char c : str.toCharArray()) {
        if (Character.isDigit(c)) {
            currentNum += c;
        } else {
            if (!currentNum.isEmpty()) {
                total += Integer.parseInt(currentNum);
                currentNum = "";
            }
        }
    }
    if (!currentNum.isEmpty()) {
        total += Integer.parseInt(currentNum);
    }
    return total;
}
```

> ⚠️ 注意：频繁字符串拼接（`+=`）会创建多个对象，效率低，**不推荐用于长字符串**

---

## 🧪 边界测试用例

| 输入字符串 | 提取的数字 | 和 |
|-----------|-----------|----|
| `"a12b3c45"` | 12, 3, 45 | 60 |
| `"123"` | 123 | 123 |
| `"abc"` | （无） | 0 |
| `"1a2b3"` | 1, 2, 3 | 6 |
| `"0a0"` | 0, 0 | 0 |
| `""` | （无） | 0 |
| `"1000x99"` | 1000, 99 | 1099 |

---

## 📚 知识点总结

| 方法 / 概念 | 说明 |
|------------|------|
| `Character.isDigit(c)` | 判断字符是否为数字（'0'-'9'）|
| `Integer.parseInt(str)` | 将数字字符串转为整数 |
| `StringBuilder.setLength(0)` | 高效清空缓冲区（比 `new StringBuilder()` 更省资源）|
| 边界处理 | 字符串以数字结尾时需额外处理 |
| 累加逻辑 | 在“非数字出现”或“字符串结束”时触发累加 |

---

## 🧠 小练习建议

1. **扩展功能**：支持负数（如 `"a-12b3"` → -12 + 3 = -9）  
   > 提示：需判断 `'-'` 后紧跟数字，且前面不是数字
2. **性能对比**：用长字符串测试 `StringBuilder` vs 字符串拼接的效率差异
3. **错误处理**：如果数字过大（超过 `int` 范围），如何处理？（可改用 `long` 或 `BigInteger`）

---

