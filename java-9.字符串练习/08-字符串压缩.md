
---

## 📌 题目：字符串压缩（Run-Length Encoding）

**题目描述**：  
实现一个方法，对字符串进行**行程长度编码（Run-Length Encoding）**：

- 将连续重复的字符压缩为 `"字符 + 出现次数"` 的形式  
- 如果压缩后的字符串**长度 ≥ 原字符串长度**，则**返回原字符串**

**示例**：
- `"aaabbc"` → `"a3b2c1"`（原长 6，压缩后 6 → **相等，返回原串** ❗）
- `"aabcccccaaa"` → `"a2b1c5a3"`（原长 11，压缩后 8 → 返回 `"a2b1c5a3"`）
- `"abc"` → `"a1b1c1"`（压缩后更长 → 返回 `"abc"`）

> ⚠️ 注意：**只有当压缩结果更短时才返回压缩串**，否则返回原串！

---

## 💡 实现思路

1. **遍历字符串**，统计每个字符的连续出现次数
2. 使用 `StringBuilder` 高效拼接压缩结果（避免频繁创建字符串）
3. 每次字符变化时，将“字符 + 次数”追加到结果中
4. 遍历结束后，**比较压缩串与原串长度**
5. 返回较短者

> 🔑 关键点：
> - 如何处理最后一组字符？
> - 数字可能多位（如 `12` 个 `'a'` → `"a12"`）

---

## ✅ 完整实现代码

```java
public class StringCompression {

    public static String compress(String str) {
        if (str == null || str.length() <= 2) {
            // 长度 ≤2 时，压缩后不可能更短（如 "aa" → "a2" 长度相同）
            return str;
        }

        StringBuilder compressed = new StringBuilder();
        int count = 1;

        for (int i = 0; i < str.length(); i++) {
            // 如果不是最后一个字符，且当前字符等于下一个
            if (i + 1 < str.length() && str.charAt(i) == str.charAt(i + 1)) {
                count++;
            } else {
                // 字符变化或到达末尾：写入当前字符和计数
                compressed.append(str.charAt(i));
                compressed.append(count);
                count = 1; // 重置计数
            }
        }

        // 只有当压缩后更短时才返回压缩结果
        return compressed.length() < str.length() ? compressed.toString() : str;
    }

    public static void main(String[] args) {
        System.out.println(compress("aaabbc"));        // "aaabbc"（压缩后长度相等，返回原串）
        System.out.println(compress("aabcccccaaa"));   // "a2b1c5a3"
        System.out.println(compress("abc"));           // "abc"
        System.out.println(compress("a"));             // "a"
        System.out.println(compress(""));              // ""
        System.out.println(compress("aaaaaaaaaa"));    // "a10"（10 是两位数！）
    }
}
```

---

## 🧪 边界测试用例

| 输入 | 压缩结果 | 是否返回压缩？ | 说明 |
|------|--------|---------------|------|
| `"aaabbc"` | `"a3b2c1"`（长度6） | ❌ 否 | 与原串等长 |
| `"aabcccccaaa"` | `"a2b1c5a3"`（长度8） | ✅ 是 | 原串长11 |
| `"abc"` | `"a1b1c1"`（长度6） | ❌ 否 | 更长 |
| `"a"` | `"a1"`（长度2） | ❌ 否 | 更长 |
| `"aaaaaaaaaa"` | `"a10"`（长度3） | ✅ 是 | 注意：`10` 是两位数！|
| `""` | `""` | — | 空串直接返回 |

---

## 📚 知识点总结

| 概念 | 说明 |
|------|------|
| `StringBuilder` | 高效拼接字符串，避免 `String` 不可变带来的性能问题 |
| 连续字符统计 | 通过比较 `s[i]` 和 `s[i+1]` 判断是否继续计数 |
| 多位数字处理 | `append(count)` 自动处理 `10`, `100` 等 |
| 长度比较 | 压缩后必须**严格更短**才使用（题目要求） |
| 边界优化 | 长度 ≤2 时可提前返回，提升效率 |

---

## 🧠 小练习建议

1. **修改规则**：即使压缩后等长也返回压缩串，观察结果变化。
2. **扩展功能**：支持解压缩（将 `"a3b2"` 还原为 `"aaabb"`）。
3. **性能挑战**：用字符数组预分配空间（进阶）。

---

