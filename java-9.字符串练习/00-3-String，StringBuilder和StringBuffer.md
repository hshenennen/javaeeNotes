这是一个非常经典且重要的问题！`String`、`StringBuilder` 和 `StringBuffer` 都用于处理文本，但它们在**可变性、线程安全性和性能**上有本质区别。下面我们从多个维度详细对比三者：

---

## 🔹 1. **可变性（Mutability）**

| 类型 | 是否可变 | 说明 |
|------|--------|------|
| `String` | ❌ 不可变 | 一旦创建，内容不能更改。任何“修改”操作都会生成新对象。 |
| `StringBuilder` | ✅ 可变 | 内容可以被修改，不会创建新对象。 |
| `StringBuffer` | ✅ 可变 | 同上，内容可变。 |

> 📌 示例：  
> ```java
> String s = "Hi";
> s = s + "!"; // 实际上创建了一个新 String 对象
> 
> StringBuilder sb = new StringBuilder("Hi");
> sb.append("!"); // 直接在原对象上修改，没有新对象
> ```

---

## 🔹 2. **线程安全性（Thread Safety）**

| 类型 | 线程安全？ | 说明 |
|------|----------|------|
| `String` | ✅ 安全 | 因为不可变，天然线程安全。多个线程读同一个 `String` 没问题。 |
| `StringBuilder` | ❌ 不安全 | 所有方法**没有同步（synchronized）**，多线程同时操作可能出错。 |
| `StringBuffer` | ✅ 安全 | 所有公开方法都加了 `synchronized`，保证线程安全。 |

> ⚠️ 举例：  
> 如果两个线程同时对一个 `StringBuilder` 调用 `append()`，结果可能混乱（如字符交错）。  
> 而 `StringBuffer` 会排队执行，保证正确性，但牺牲性能。

---

## 🔹 3. **性能（Performance）**

| 类型              | 性能          | 原因                               |
| --------------- | ----------- | -------------------------------- |
| `String`        | ❌ 最慢（频繁修改时） | 每次修改都创建新对象，GC 压力大。               |
| `StringBuilder` | ✅ 最快        | 无同步开销，直接操作内部数组。                  |
| `StringBuffer`  | ⚠️ 较慢       | 因为每个方法都要获取锁（synchronized），有额外开销。 |

> 💡 实测：在单线程下，`StringBuilder` 比 `StringBuffer` 快约 10%~30%；比反复拼接 `String` 快几十倍以上。

---

## 🔹 4. **底层实现**

三者底层都使用 `char[]`（字符数组）存储数据：

- `String` 的 `char[]` 被 `final` 修饰，不可更改。
- `StringBuilder` 和 `StringBuffer` 的 `char[]` 可动态扩容（类似 `ArrayList`）。

> 📌 注意：从 Java 9 开始，`String` 内部改用 `byte[]` + 编码标识来节省内存，但对外行为不变。

---

## 🔹 5. **使用场景建议**

| 场景 | 推荐类型 |
|------|--------|
| 字符串内容固定，或只读 | `String` |
| 单线程下频繁拼接/修改字符串（如循环中构建 SQL、日志） | `StringBuilder` ✅（最常用） |
| 多线程环境下需要共享并修改同一个字符串缓冲区 | `StringBuffer`（较少见） |

> 🌰 现实中 95% 以上的字符串构建场景都用 `StringBuilder`！

---

## 🔹 代码对比示例

```java
// 1. String（低效）
String s = "";
for (int i = 0; i < 1000; i++) {
    s += i; // 每次都 new 一个 String！
}

// 2. StringBuilder（高效，单线程）
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 1000; i++) {
    sb.append(i);
}
String result = sb.toString();

// 3. StringBuffer（高效但带锁，多线程安全）
StringBuffer sbf = new StringBuffer();
for (int i = 0; i < 1000; i++) {
    sbf.append(i);
}
String result2 = sbf.toString();
```

---

## ✅ 总结表格

| 特性 | `String` | `StringBuilder` | `StringBuffer` |
|------|--------|------------------|----------------|
| 可变性 | 不可变 | 可变 | 可变 |
| 线程安全 | 是 | 否 | 是 |
| 性能 | 低（频繁修改时） | 高 | 中（因同步开销） |
| 适用场景 | 常量、少量操作 | 单线程大量字符串操作 | 多线程共享字符串缓冲区 |
| 是否推荐日常使用 | 是（只读场景） | ✅ 强烈推荐（构建字符串） | 很少需要 |

---

如果你是在做算法题、写工具类、拼接日志、生成 JSON/XML 等，**几乎总是选择 `StringBuilder`**！