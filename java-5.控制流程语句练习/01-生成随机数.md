
---

# Java 生成随机数（Random Number）

> [!info] 学习目标
> - 掌握 `Math.random()` 和 `Random` 类的使用  
> - 能生成指定范围的整数或小数  
> - 理解两种方式的区别与适用场景  
> - 避免常见错误（如范围错误、类型转换问题）

---

## 📚 两种主要方式

| 方式 | 类/方法 | 特点 | 适用场景 |
|------|--------|------|--------|
| ✅ `Math.random()` | `java.lang.Math` | 简单、无需导入、返回 `double` | 快速生成 0~1 之间的随机小数 |
| ✅ `Random` 类 | `java.util.Random` | 功能强大、可重复使用、支持多种类型 | 需要整数、多次生成、可控种子 |

---

## 1. 使用 `Math.random()`（推荐简单场景）

### 🔹 基本用法
```java
double r = Math.random(); // 返回 [0.0, 1.0) 之间的 double
System.out.println(r);    // 例如：0.732456789
```

### 🔹 生成指定范围的整数（常用公式）
> **公式**：`(int)(Math.random() * (max - min + 1)) + min`

#### 示例：生成 1~100 的随机整数
```java
int num = (int)(Math.random() * 100) + 1;
// 解释：
// Math.random() * 100 → [0.0, 100.0)
// (int) 强制转换 → [0, 99]
// +1 → [1, 100]
```

#### 示例：生成 50~100 的随机整数
```java
int num = (int)(Math.random() * 51) + 50; // 51 = 100 - 50 + 1
```

> ⚠️ 注意：**`+1` 是关键**！否则无法包含最大值。

---

## 2. 使用 `Random` 类（推荐通用场景）

### 🔹 基本步骤
```java
import java.util.Random; // 必须导入

Random rand = new Random(); // 创建 Random 对象

// 生成 0~99 的整数
int n1 = rand.nextInt(100); // [0, 100)

// 生成 1~100 的整数
int n2 = rand.nextInt(100) + 1; // [1, 100]
```

### 🔹 常用方法
| 方法 | 返回值 | 范围 |
|------|--------|------|
| `nextInt()` | `int` | 所有 int 值（-2³¹ ~ 2³¹-1） |
| `nextInt(int bound)` | `int` | `[0, bound)` |
| `nextDouble()` | `double` | `[0.0, 1.0)` |
| `nextBoolean()` | `boolean` | `true` 或 `false` |

### 🔹 生成任意范围整数（推荐写法）
```java
// 生成 [min, max] 之间的整数
public static int randomInt(int min, int max) {
    Random rand = new Random();
    return rand.nextInt(max - min + 1) + min;
}

// 使用
int dice = randomInt(1, 6); // 模拟骰子
```

> 💡 提示：**`Random` 对象可复用**，建议在循环外创建一次。

---

## 3. 对比：`Math.random()` vs `Random`

| 特性 | `Math.random()` | `Random` 类 |
|------|------------------|-------------|
| 是否需要导入 | ❌ 不需要 | ✅ 需要 `import java.util.Random;` |
| 返回类型 | `double` | 支持 `int`, `double`, `boolean` 等 |
| 性能 | 每次调用新建内部 Random | 可复用，性能更好 |
| 可控性 | 无法设置种子 | 可通过 `new Random(seed)` 设置种子（用于测试） |
| 代码简洁性 | 简单一行 | 需创建对象 |

> ✅ **建议**：
> - 简单脚本、快速测试 → 用 `Math.random()`
> - 游戏、算法、多次生成 → 用 `Random` 类

---

## 4. 实用示例

### 🌰 示例 1：猜数字游戏（1~100）
```java
import java.util.Random;
Random rand = new Random();
int secret = rand.nextInt(100) + 1; // 1~100
```

### 🌰 示例 2：随机生成 10 个 1~6 的骰子点数
```java
Random rand = new Random();
for (int i = 0; i < 10; i++) {
    System.out.print(rand.nextInt(6) + 1 + " ");
}
// 输出：3 6 1 4 2 5 6 1 3 2
```

### 🌰 示例 3：随机布尔值（开关、真假）
```java
Random rand = new Random();
boolean flag = rand.nextBoolean();
if (flag) {
    System.out.println("开启");
} else {
    System.out.println("关闭");
}
```

---

## 5. ⚠️ 常见错误与注意事项

| 错误 | 说明 | 修正 |
|------|------|------|
| `Math.random(100)` | `Math.random()` 无参数 | 用 `Math.random() * 100` |
| 忘记 `+1` | `rand.nextInt(100)` 生成 0~99 | 要 1~100 → `+1` |
| 类型转换错误 | `(int)Math.random() * 100` → 先转 int（0）再乘 | 用 `(int)(Math.random() * 100)` |
| 在循环内重复创建 Random | 影响随机性（尤其短时间） | 在循环外创建一次 |

---

## 6. 小练习

以下代码可能输出什么？
```java
int x = (int)(Math.random() * 3) + 5;
System.out.println(x);
```

<details>
<summary>✅ 答案</summary>

可能输出：**5、6 或 7**

> 💡 解析：
> - `Math.random() * 3` → [0.0, 3.0)
> - `(int)` 转换 → 0, 1, 2
> - `+5` → 5, 6, 7
</details>

---


