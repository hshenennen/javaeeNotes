
---

# 📅 根据年月判断天数（含闰年判断）

> [!info] 学习目标
> - 综合运用 `if-else` 条件分支  
> - 掌握月份天数规则与闰年逻辑  
> - 实现用户交互式程序（`Scanner`）  
> - **不使用自定义方法**，所有逻辑在 `main` 中完成  
> - 为后续学习（如数组、方法封装）打下基础

> 🏷️ 标签：`#java/基础` `#条件语句` `#编程/练习` `#日期处理`

---

## 🧩 问题描述

编写一个 Java 程序：
- 用户输入 **年份** 和 **月份（1~12）**
- 程序输出该月的 **天数**
  - **大月**（1,3,5,7,8,10,12）→ 31 天  
  - **小月**（4,6,9,11）→ 30 天  
  - **2 月** → 平年 28 天，闰年 29 天
---

## ✅ 完整代码

```java
import java.util.Scanner;

public class DaysInMonth {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("请输入年份: ");
        int year = scanner.nextInt();

        System.out.print("请输入月份（1-12）: ");
        int month = scanner.nextInt();

        // 校验月份合法性
        if (month < 1 || month > 12) {
            System.out.println("❌ 月份必须在 1 到 12 之间！");
            scanner.close();
            return;
        }

        int days = 0;

        // 判断月份类型
        if (month == 1 || month == 3 || month == 5 || month == 7 ||
            month == 8 || month == 10 || month == 12) {
            days = 31;
        } else if (month == 4 || month == 6 || month == 9 || month == 11) {
            days = 30;
        } else if (month == 2) {
            // 闰年判断：(能被4整除且不能被100整除) 或 (能被400整除)
            if ((year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)) {
                days = 29;
            } else {
                days = 28;
            }
        }

        System.out.println(year + " 年 " + month + " 月有 " + days + " 天。");
        scanner.close();
    }
}
```

> 💡 **特点**：  
> - 包含输入校验（月份范围）  
> - 闰年判断使用标准公式

---

## 📚 核心知识点拆解

### 1. 月份分类逻辑

| 类型 | 月份 | 天数 |
|------|------|------|
| **大月** | 1, 3, 5, 7, 8, 10, 12 | 31 |
| **小月** | 4, 6, 9, 11 | 30 |
| **特殊月** | 2 | 28 或 29 |

> 🧠 记忆口诀：  
> **“一三五七八十腊（12），三十一天永不差；四六九冬（11）三十整；平年二月二十八，闰年再把一日加。”**

---

### 2. 闰年判断公式（嵌套在 2 月逻辑中）

```java
(year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)
```

> ✅ 正确覆盖：
> - 2024 → 闰年（%4=0，%100≠0）  
> - 1900 → 平年（%100=0，但%400≠0）  
> - 2000 → 闰年（%400=0）

---

### 3. 输入校验（健壮性）

```java
if (month < 1 || month > 12) {
    System.out.println("❌ 月份必须在 1 到 12 之间！");
    return; // 提前结束程序
}
```

> ⚠️ 若不校验，输入 `13` 会导致 `days = 0`，输出错误结果。

---

## 🔍 可能的改进方向（未来学习）

虽然本题要求“不用方法”，但学完后可尝试以下优化：

<details>
<summary>💡 进阶思路（供参考）</summary>

### ✅ 用数组简化月份判断
```java
int[] daysInMonth = {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};
int days = daysInMonth[month - 1];
if (month == 2 && isLeapYear(year)) days = 29;
```

### ✅ 封装闰年判断为方法
```java
public static boolean isLeapYear(int year) {
    return (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0);
}
```

### ✅ 支持循环查询
```java
String choice;
do {
    // 主逻辑
    System.out.print("继续查询？(y/n): ");
    choice = scanner.next();
} while (choice.equals("y"));
```

</details>

---

## 🧪 测试用例建议

| 年份 | 月份 | 期望输出 |
|------|------|--------|
| 2024 | 2 | 29 |
| 1900 | 2 | 28 |
| 2000 | 2 | 29 |
| 2023 | 4 | 30 |
| 2023 | 12 | 31 |
| 2023 | 13 | ❌ 提示错误 |

---




