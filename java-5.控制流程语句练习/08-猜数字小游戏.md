

---

# 🎮 Java 猜数字小游戏（Guess the Number）

> [!info] 游戏规则
> - 程序随机生成一个 1~100 的整数  
> - 玩家通过控制台输入猜测的数字  
> - 程序提示“太大了”、“太小了”或“恭喜猜中！”  
> - 统计玩家尝试次数  
> - 支持非法输入处理（如输入字母）  
> - 可选择是否再玩一局

---

## ✅ 完整代码（可直接运行）

```java
import java.util.Random;
import java.util.Scanner;

public class GuessNumberGame {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        Random random = new Random();
        boolean playAgain = true;

        System.out.println("🎮 欢迎来到【猜数字小游戏】！");
        System.out.println("我已经想好了一个 1 到 100 之间的整数，你来猜吧！");

        while (playAgain) {
            int target = random.nextInt(100) + 1; // 1~100
            int attempts = 0;
            boolean guessed = false;

            while (!guessed) {
                System.out.print("请输入你的猜测: ");

                // 输入验证：确保是整数
                if (scanner.hasNextInt()) {
                    int guess = scanner.nextInt();
                    attempts++;

                    if (guess < 1 || guess > 100) {
                        System.out.println("⚠️ 请输入 1 到 100 之间的数字！");
                        continue;
                    }

                    if (guess == target) {
                        System.out.println("🎉 恭喜你！猜中了！答案就是 " + target);
                        System.out.println("你总共尝试了 " + attempts + " 次。");
                        guessed = true;
                    } else if (guess < target) {
                        System.out.println("📈 太小了，再大一点！");
                    } else {
                        System.out.println("📉 太大了，再小一点！");
                    }
                } else {
                    // 清除非法输入（如字母）
                    String invalid = scanner.next();
                    System.out.println("❌ 输入无效！请输入一个整数。");
                }
            }

            // 询问是否再玩一局
            System.out.print("想再玩一次吗？(y/n): ");
            String response = scanner.next().trim().toLowerCase();
            playAgain = response.equals("y") || response.equals("yes");
            if (playAgain) {
                System.out.println("\n🔄 新游戏开始！我已经想好了新数字～");
            }
        }

        System.out.println("👋 谢谢游玩！再见！");
        scanner.close();
    }
}
```

---

## 🔍 核心知识点解析

| 技术点 | 说明 |
|--------|------|
| `Random` 类 | `random.nextInt(100)` 生成 0~99，+1 → 1~100 |
| `Scanner` 输入 | 使用 `hasNextInt()` 验证输入类型 |
| 输入清理 | `scanner.next()` 清除非法 token（如 `"abc"`） |
| 循环控制 | 外层 `while(playAgain)` 控制多局游戏，内层 `while(!guessed)` 控制单局 |
| 条件判断 | `if-else if-else` 实现大小比较 |
| 用户体验 | 提示清晰、支持重玩、处理边界输入 |

---

## 💡 扩展建议（进阶练习）

你可以尝试以下改进，巩固所学知识：

### 1. **限制猜测次数**
```java
final int MAX_ATTEMPTS = 7;
if (attempts >= MAX_ATTEMPTS && !guessed) {
    System.out.println("💀 次数用尽！正确答案是 " + target);
    break;
}
```

### 2. **难度选择**
```java
System.out.println("请选择难度：1(1-50), 2(1-100), 3(1-1000)");
int range = switch (level) {
    case 1 -> 50;
    case 2 -> 100;
    case 3 -> 1000;
    default -> 100;
};
int target = random.nextInt(range) + 1;
```

### 3. **记录历史成绩**
使用数组或 `ArrayList` 存储每局的尝试次数，最后显示最佳成绩。

### 4. **添加计时功能**
```java
long startTime = System.currentTimeMillis();
// ... 游戏结束时
long endTime = System.currentTimeMillis();
System.out.println("用时: " + (endTime - startTime)/1000 + " 秒");
```

---

## 📝 小结

这个小游戏虽然简单，但涵盖了 Java 编程的多个核心概念：
- 用户交互（`Scanner`）
- 随机数生成（`Random`）
- 流程控制（`while`, `if`）
- 异常预防（输入验证）
- 代码结构（模块化、可读性）

> ✅ **建议**：亲手敲一遍代码，不要复制粘贴！尝试加入自己的创意，比如：
> - 添加“作弊模式”（显示答案）
> - 用颜色高亮提示（需 ANSI 转义码）
> - 图形界面版（后续学 Swing/JavaFX）

---



