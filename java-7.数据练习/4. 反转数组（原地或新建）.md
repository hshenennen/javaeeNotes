当然可以！下面是 **两种 反转数组** 的实现方式：

- ✅ **方式一：新建一个数组（更简单、安全）**  
- ✅ **方式二：原地反转（不额外占用空间，直接修改原数组）**

---

## 📌 4. 反转数组（两种实现）

### ✅ 方式一：新建数组（推荐初学者）

```java
public class ReverseArrayNew {
    public static void main(String[] args) {
        int[] original = {1, 2, 3, 4, 5};
        int[] reversed = new int[original.length]; // 创建新数组

        // 从后往前复制元素
        for (int i = 0; i < original.length; i++) {
            reversed[i] = original[original.length - 1 - i];
        }

        // 输出原数组
        System.out.print("原数组: ");
        for (int i = 0; i < original.length; i++) {
            System.out.print(original[i] + " ");
        }

        // 输出反转后的数组
        System.out.print("\n反转后: ");
        for (int i = 0; i < reversed.length; i++) {
            System.out.print(reversed[i] + " ");
        }
    }
}
```

> ✅ **优点**：逻辑清晰，不会改变原数组。  
> 💡 输出示例：
> ```
> 原数组: 1 2 3 4 5 
> 反转后: 5 4 3 2 1 
> ```

---

### ✅ 方式二：原地反转（双指针法）

```java
public class ReverseArrayInPlace {
    public static void main(String[] args) {
        int[] arr = {10, 20, 30, 40, 50};

        // 输出原数组
        System.out.print("原数组: ");
        for (int i = 0; i < arr.length; i++) {
            System.out.print(arr[i] + " ");
        }

        // 原地反转：首尾交换，向中间靠拢
        int left = 0;
        int right = arr.length - 1;
        while (left < right) {
            // 交换 arr[left] 和 arr[right]
            int temp = arr[left];
            arr[left] = arr[right];
            arr[right] = temp;

            left++;
            right--;
        }

        // 输出反转后的数组（原数组已被修改）
        System.out.print("\n反转后: ");
        for (int i = 0; i < arr.length; i++) {
            System.out.print(arr[i] + " ");
        }
    }
}
```

> ✅ **优点**：节省内存，空间复杂度 O(1)。  
> ⚠️ **注意**：原数组内容会被改变！  
> 💡 输出示例：
> ```
> 原数组: 10 20 30 40 50 
> 反转后: 50 40 30 20 10 
> ```

---

### 📝 小结（Obsidian 笔记建议）

| 方式       | 是否修改原数组 | 额外空间 | 难度 |
|------------|----------------|--------|------|
| 新建数组   | ❌ 否           | O(n)   | ⭐   |
| 原地反转   | ✅ 是           | O(1)   | ⭐⭐  |

> 初学建议先掌握**新建数组**的方式，理解后再学习**原地反转**。

---

