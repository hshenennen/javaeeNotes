
---

### 📌 Java 数组练习：求数组元素总和与平均值

```java
public class ArraySumAndAverage {
    public static void main(String[] args) {
        // 定义一个整型数组（你可以随意修改里面的数值）
        int[] numbers = {15, 25, 35, 45, 55};

        // 初始化总和为 0
        int sum = 0;

        // 遍历数组，累加每个元素
        for (int i = 0; i < numbers.length; i++) {
            sum += numbers[i];
        }

        // 计算平均值（注意：转为 double 避免整数除法）
        double average = (double) sum / numbers.length;

        // 输出结果
        System.out.println("数组元素总和: " + sum);
        System.out.println("数组元素平均值: " + average);
    }
}
```

#### 🔍 说明：
- 使用 `for` 循环遍历数组，逐个累加。
- **`(double) sum`** 是为了确保除法结果是小数（例如：100 / 3 = 33.333...，而不是 33）。
- 所有逻辑都在 `main` 方法中，**没有定义额外方法**，符合初学练习要求。

#### 💡 示例输出（以上数组）：
```
数组元素总和: 175
数组元素平均值: 35.0
```

---

