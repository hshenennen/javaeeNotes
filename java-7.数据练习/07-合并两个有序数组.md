

> 📌 假设两个输入数组 **都是升序排列**（如：`{1, 3, 5}` 和 `{2, 4, 6, 8}`）

---

### 📌 合并两个有序数组（双指针法）

```java
public class MergeSortedArrays {
    public static void main(String[] args) {
        // 两个已排序的升序数组
        int[] arr1 = {1, 3, 5, 7};
        int[] arr2 = {2, 4, 6, 8, 10};

        // 创建一个新数组，长度为两个数组长度之和
        int[] merged = new int[arr1.length + arr2.length];

        // 三个指针：i 指向 arr1，j 指向 arr2，k 指向 merged
        int i = 0, j = 0, k = 0;

        // 同时遍历两个数组，比较元素大小，小的先放入 merged
        while (i < arr1.length && j < arr2.length) {
            if (arr1[i] <= arr2[j]) {
                merged[k] = arr1[i];
                i++;
            } else {
                merged[k] = arr2[j];
                j++;
            }
            k++;
        }

        // 如果 arr1 还有剩余元素，全部复制过去
        while (i < arr1.length) {
            merged[k] = arr1[i];
            i++;
            k++;
        }

        // 如果 arr2 还有剩余元素，全部复制过去
        while (j < arr2.length) {
            merged[k] = arr2[j];
            j++;
            k++;
        }

        // 输出合并后的数组
        System.out.print("合并后的有序数组: ");
        for (int idx = 0; idx < merged.length; idx++) {
            System.out.print(merged[idx] + " ");
        }
        System.out.println();
    }
}
```

#### 💡 示例输出：
```
合并后的有序数组: 1 2 3 4 5 6 7 8 10 
```

---

### 🔍 代码说明：
- 使用 **双指针（i 和 j）** 分别遍历两个数组。
- 每次将 **较小的元素** 放入结果数组 `merged`，并移动对应指针。
- 最后处理 **剩余元素**（因为两个数组长度可能不同）。
- 整个过程 **时间复杂度 O(m + n)**，高效且经典。

---

### ✅ 为什么适合初学者？
- 没有用 `Arrays.sort()` 或集合类，纯靠逻辑合并。
- 是“归并排序”中“合并”步骤的核心思想。
- 练习指针控制、边界处理和顺序思维的好题目。

---

### 🧪 小练习建议：
- 尝试 `arr1 = {}`（空数组）和 `arr2 = {1,2}`，看是否正常
- 把数组改成降序，思考如何修改代码（提示：比较符号反向）