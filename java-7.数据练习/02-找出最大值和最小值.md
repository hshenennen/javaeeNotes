
---

### 📌 Java 数组练习：找出最大值和最小值

```java
public class FindMinMax {
    public static void main(String[] args) {
        // 定义一个整型数组（你可以修改这些数值进行测试）
        int[] arr = {42, -17, 88, 0, 23, -5, 100};

        // 假设第一个元素既是最大值也是最小值
        int max = arr[0];
        int min = arr[0];

        // 从第二个元素开始遍历数组
        for (int i = 1; i < arr.length; i++) {
            if (arr[i] > max) {
                max = arr[i];  // 更新最大值
            }
            if (arr[i] < min) {
                min = arr[i];  // 更新最小值
            }
        }

        // 输出结果
        System.out.println("数组中的最大值是: " + max);
        System.out.println("数组中的最小值是: " + min);
    }
}
```

#### 🔍 说明：
- 初始化 `max` 和 `min` 为 `arr[0]` 是安全的做法（前提是数组非空）。
- 循环从索引 `1` 开始，避免重复比较第一个元素。
- 使用两个独立的 `if`（而不是 `else if`），因为同一个元素不可能同时更新最大和最小，但分开写逻辑更清晰、通用。

#### 💡 示例输出（以上数组）：
```
数组中的最大值是: 100
数组中的最小值是: -17
```

---

⚠️ **注意**：此代码假设数组至少有一个元素。如果数组可能为空，需要先检查 `arr.length > 0`，但初学阶段通常默认数组非空。