
---

### 📌 Java 数组练习：判断数组中是否包含某个特定值

```java
public class ArrayContainsValue {
    public static void main(String[] args) {
        // 定义一个整型数组
        int[] arr = {10, 25, 33, 42, 58, 67, 75};

        // 要查找的目标值（你可以修改这个值进行测试）
        int target = 42;

        // 假设没找到
        boolean found = false;

        // 遍历数组，逐个比较
        for (int i = 0; i < arr.length; i++) {
            if (arr[i] == target) {
                found = true;   // 找到了！
                break;          // 提前退出循环，提高效率
            }
        }

        // 输出结果
        if (found) {
            System.out.println("数组中包含值: " + target);
        } else {
            System.out.println("数组中不包含值: " + target);
        }
    }
}
```

#### 🔍 说明：
- 使用 `boolean` 变量 `found` 记录是否找到目标值。
- 一旦找到，立即使用 `break` 跳出循环，避免不必要的比较。
- 逻辑清晰，适合初学者理解“查找”类问题的基本思路。

#### 💡 示例输出（当 `target = 42` 时）：
```
数组中包含值: 42
```

如果将 `target` 改为 `99`，输出将是：
```
数组中不包含值: 99
```


---

### 🧪 扩展建议（可选练习）：
- 尝试改成 `String[]` 数组，查找某个字符串（注意用 `.equals()` 比较）。
- 不使用 `break`，看看结果是否一样（效率更低，但逻辑仍正确）。
- 输出目标值在数组中的**位置（索引）**，而不仅仅是“是否包含”。
