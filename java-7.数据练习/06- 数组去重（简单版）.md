

---

### 📌 数组去重（简单版）—— 保留顺序，仅用基础数组

```java
public class ArrayDeduplicateSimple {
    public static void main(String[] args) {
        int[] original = {1, 2, 2, 3, 4, 4, 5, 2, 6, 1};

        // 创建一个足够大的临时数组（最坏情况：无重复）
        int[] temp = new int[original.length];
        int newSize = 0; // 记录去重后实际元素个数

        // 遍历原数组
        for (int i = 0; i < original.length; i++) {
            boolean isDuplicate = false;

            // 检查当前元素是否已在 temp 中出现过
            for (int j = 0; j < newSize; j++) {
                if (original[i] == temp[j]) {
                    isDuplicate = true;
                    break;
                }
            }

            // 如果不是重复的，就加入 temp
            if (!isDuplicate) {
                temp[newSize] = original[i];
                newSize++;
            }
        }

        // 输出去重后的结果（只输出前 newSize 个元素）
        System.out.print("去重后的数组: ");
        for (int i = 0; i < newSize; i++) {
            System.out.print(temp[i] + " ");
        }
        System.out.println();
    }
}
```

#### 💡 示例输出：
```
去重后的数组: 1 2 3 4 5 6 
```

---

### 🔍 代码说明：
- `temp` 数组用于存放不重复的元素。
- `newSize` 是去重后的真实长度（因为 `temp` 可能有未使用的空间）。
- 内层循环只检查 `0` 到 `newSize - 1`，确保只和已加入的元素比较。
- **保留了元素首次出现的顺序**。

---

### ✅ 优点 & 适用场景：
- 只用基础语法（数组 + 循环 + 条件），无任何高级 API。
- 逻辑直观，适合理解“去重”本质。
- 适合小规模数据练习。

### ⚠️ 局限性（了解即可）：
- 效率较低（每加一个元素都要遍历已有结果）。
- 不能直接得到“新数组对象”（需手动截取 `temp[0..newSize-1]`，但初学可忽略）。

---
