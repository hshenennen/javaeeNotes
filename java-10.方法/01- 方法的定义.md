 
---

# Java 方法的定义

> **标签**：`#Java/方法` `#Java/基础语法`

## 什么是方法？

**方法（Method）** 是一段封装了特定功能的代码块，用于执行某项任务。  
通过定义方法，可以实现 **代码复用**、**逻辑分离** 和 **程序模块化**。

> 📌 在 Java 中，**所有方法必须定义在类（class）内部**，不能独立存在。

---

## 方法的完整语法结构

```java
[访问修饰符] [其他修饰符] 返回类型 方法名(参数列表) [throws 异常列表] {
    // 方法体（执行的具体代码）
    [return 表达式;] // 如果返回类型不是 void
}
```

### 各组成部分详解：

| 组成部分 | 说明 | 是否必需 |
|----------|------|--------|
| **访问修饰符** | 控制方法的可见性，如 `public`、`private`、`protected` 或默认（无） | 否（但建议显式声明） |
| **其他修饰符** | 如 `static`、`final`、`abstract` 等 | 否 |
| **返回类型** | 方法执行后返回的数据类型（如 `int`、`String`），若无返回值则为 `void` | ✅ 必需 |
| **方法名** | 遵循 Java 标识符规则，推荐使用**驼峰命名法**（如 `calculateArea`） | ✅ 必需 |
| **参数列表** | 用逗号分隔的“类型 + 变量名”对，如 `(int a, String name)`；无参数则为空 `()` | 否（可为空） |
| **方法体** | 用 `{}` 包裹的代码块，包含具体逻辑 | ✅ 必需（除非是抽象方法） |
| **return 语句** | 返回结果并结束方法（`void` 方法可省略或仅写 `return;`） | 视返回类型而定 |

---

## 示例：不同形式的方法定义

### 1. 最简方法（无参、无返回、public）
```java
public void sayHello() {
    System.out.println("Hello!");
}
```

### 2. 带参数和返回值
```java
public int multiply(int x, int y) {
    return x * y;
}
```

### 3. 静态工具方法
```java
public static double circleArea(double radius) {
    return 3.14159 * radius * radius;
}
```

### 4. 带多个参数
```java
public String introduce(String name, int age) {
    return "My name is " + name + ", I'm " + age + " years old.";
}
```

---

## 注意事项 ⚠️

1. **方法名不能与类中的其他方法仅靠返回类型区分**  
   ❌ 错误示例：
   ```java
   int getValue() { ... }
   double getValue() { ... } // 编译错误！不能仅靠返回类型重载
   ```

2. **`void` 方法不能返回值**  
   ❌ 错误：
   ```java
   public void print() {
       return "hello"; // 编译错误！
   }
   ```

3. **非 `void` 方法必须在所有执行路径上返回值**  
   ✅ 正确：
   ```java
   public int sign(int x) {
       if (x > 0) return 1;
       else if (x < 0) return -1;
       else return 0; // 所有分支都有 return
   }
   ```

4. **方法不能嵌套定义**（Java 不支持内部方法）  
   ❌ 不能在一个方法内部再定义另一个方法。

---

## 方法定义 vs 方法调用

| 概念 | 说明 |
|------|------|
| **定义（Declaration/Definition）** | 编写方法的结构和逻辑（“怎么做”） |
| **调用（Invocation）** | 使用方法名 + 参数执行该方法（“请做这件事”） |

```java
// 定义
public static int add(int a, int b) {
    return a + b;
}

// 调用
int result = add(3, 4); // result = 7
```


---

> 💡 **小技巧**：在 Obsidian 中，你可以为“方法定义”创建一个代码片段模板，快速生成标准方法结构！

```java
public returnType methodName(paramType param) {
    // TODO: 实现逻辑
    return value;
}
```

保存为模板，提高编码效率！

---
