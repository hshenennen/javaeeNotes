
---

# Java 方法重载（Method Overloading）

> **标签**：`#Java/方法` `#Java/面向对象` `#方法重载`

## 什么是方法重载？

**方法重载** 是指在**同一个类中**，可以定义**多个同名方法**，只要它们的 **参数列表不同**（参数的类型、数量或顺序不同）。

- ✅ 允许使用相同的方法名处理不同类型或数量的输入。
- ❌ **不能仅通过返回类型或访问修饰符不同来重载**。
- 编译器根据调用时传入的**实参类型和数量**决定调用哪个方法（**编译时多态**）。

> 📌 方法重载是 Java 实现 **多态性（Polymorphism）** 的一种方式（静态多态）。

---

## 重载的判断标准

两个方法要构成重载，必须满足：

| 条件 | 是否影响重载 |
|------|-------------|
| 方法名相同 | ✅ 必须相同 |
| 参数列表不同（类型/数量/顺序） | ✅ **关键条件** |
| 返回类型不同 | ❌ 不构成重载 |
| 访问修饰符不同 | ❌ 不构成重载 |
| `static` / 非 `static` | ❌ 不能仅靠这个区分 |

---

## 示例：合法的重载

```java
public class Calculator {

    // 1. 参数数量不同
    public int add(int a, int b) {
        return a + b;
    }

    public int add(int a, int b, int c) {
        return a + b + c;
    }

    // 2. 参数类型不同
    public double add(double a, double b) {
        return a + b;
    }

    // 3. 参数顺序不同（仅当类型不同时有效）
    public void printInfo(String name, int age) {
        System.out.println(name + " is " + age + " years old.");
    }

    public void printInfo(int age, String name) {
        System.out.println("Age: " + age + ", Name: " + name);
    }
}
```

### 调用示例：
```java
Calculator calc = new Calculator();
calc.add(2, 3);        // 调用 int add(int, int)
calc.add(2.5, 3.1);    // 调用 double add(double, double)
calc.add(1, 2, 3);     // 调用 int add(int, int, int)
```

---

## 常见错误 ❌

### 1. 仅返回类型不同 → **编译错误**
```java
int getValue() { return 1; }
double getValue() { return 1.0; } // ❌ 编译失败！
```

### 2. 仅参数名不同 → **不是重载**
```java
void display(int x) { ... }
void display(int y) { ... } // ❌ 实际是重复定义，编译错误！
```

### 3. 自动类型转换导致歧义
```java
void test(int a) { ... }
void test(long a) { ... }

// 调用：
test(5); // ✅ 调用 int 版本（精确匹配优先）

// 但如果同时有：
void test(float a) { ... }
void test(long a) { ... }
test(5); // ❓ 5 可转为 long 或 float，可能产生歧义（实际 Java 有明确转换规则，但需谨慎）
```

> 💡 Java 的参数匹配优先级：**精确匹配 > 自动拓宽（如 int → long）> 自动装箱 > 可变参数**

---

## 重载 vs 重写（对比）

| 特性 | 方法重载（Overloading） | 方法重写（Overriding） |
|------|------------------------|------------------------|
| 发生位置 | 同一个类中 | 子类与父类之间 |
| 方法名 | 相同 | 相同 |
| 参数列表 | **必须不同** | **必须相同** |
| 返回类型 | 可不同（但非判断依据） | 必须相同或协变 |
| 多态类型 | 编译时多态（静态） | 运行时多态（动态） |
| 关键字 | 无 | 常用 `@Override` 注解 |

> 🔗 参见：[[Java 方法重写（Override）]]

---

## 最佳实践 ✅

1. **保持语义一致**：重载的方法应完成相似功能（如都是“加法”）。
2. **避免过度重载**：太多重载版本会降低代码可读性。
3. **优先使用清晰的方法名**：如果功能差异大，不如用不同名字（如 `calculateAreaByRadius` vs `calculateAreaByDiameter`）。
4. **慎用可变参数重载**：容易引发调用歧义。

---

## 练习建议

1. 编写一个 `print` 方法，分别支持打印 `int`、`String` 和 `int[]`。
2. 尝试重载一个 `max` 方法，支持两个 `int`、三个 `int`、两个 `double`。
3. 故意制造一个重载冲突，观察编译器报错信息。


---

> 💡 **Obsidian 技巧**：你可以用 `^overload-example` 为代码块添加锚点，方便在其他笔记中引用！

```java
public void log(String msg) { ... }
public void log(String msg, int level) { ... }
^overload-example
```

掌握方法重载，是写出灵活、易用 API 的重要一步！继续加油！