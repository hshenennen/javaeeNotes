
---

## 📌 对象的封装（Encapsulation）

### ✅ 定义
> **封装（Encapsulation）** 是面向对象编程（OOP）的四大基本特性之一，指**将对象的内部状态（数据）和行为（方法）组合在一起，并对外隐藏实现细节，仅通过公开接口与外界交互**。

- 核心思想：**“高内聚，低耦合”**
- 目的：**保护数据安全**、**提高代码可维护性**

> 💡 简单理解：  
> 就像手机——你只能通过屏幕和按钮（公开接口）操作它，但看不到内部电路（私有实现）。

---

### 🔒 封装的实现方式（Java 中）

| 步骤 | 说明 |
|------|------|
| **1. 使用 `private` 修饰成员变量** | 防止外部直接访问或修改对象状态 |
| **2. 提供 `public` 的 getter/setter 方法** | 控制对数据的读取和修改（可加入校验逻辑） |
| **3. 将内部实现细节隐藏** | 外部只关心“能做什么”，不关心“怎么做” |

---

### 💻 示例：未封装 vs 封装

#### ❌ 未封装（不安全）
```java
class Student {
    public String name;  // 外部可随意修改
    public int age;
}

// 使用
Student s = new Student();
s.age = -5; // ❌ 年龄为负数！不合理但无法阻止
```

#### ✅ 封装后（安全可控）
```java
public class Student {
    // 1. 私有化字段
    private String name;
    private int age;

    // 2. 提供公共访问方法（getter/setter）
    public String getName() {
        return name;
    }

    public void setName(String name) {
        if (name != null && !name.trim().isEmpty()) {
            this.name = name;
        }
    }

    public int getAge() {
        return age;
    }

    public void setAge(int age) {
        if (age >= 0 && age <= 150) {
            this.age = age; // ✅ 加入校验逻辑
        }
    }
}
```

```java
// 使用
Student s = new Student();
s.setAge(-5);   // ❌ 无效，不会设置
s.setAge(18);   // ✅ 合法，成功设置
```

---

### ✅ 封装的好处

| 好处 | 说明 |
|------|------|
| **数据安全** | 防止非法值（如负年龄、空姓名） |
| **灵活性高** | 内部实现可修改，不影响外部调用（只要接口不变） |
| **易于维护** | 错误集中在 setter/getter 中，便于调试 |
| **符合 JavaBean 规范** | 框架（如 Spring、Hibernate）依赖标准封装结构 |

---

### 🛠️ 自动生成 getter/setter（开发技巧）

- **IDE 快捷键**：
  - IntelliJ IDEA：`Alt + Insert` → 选择 `Getter and Setter`
  - Eclipse：`Source` → `Generate Getters and Setters`
- **Lombok 注解**（进阶）：
  ```java
  @Data  // 自动生成 getter、setter、toString 等
  public class Student {
      private String name;
      private int age;
  }
  ```

---

### 🔁 封装与其他 OOP 特性的关系

| 特性 | 与封装的关系 |
|------|-------------|
| **继承（Inheritance）** | 子类不能直接访问父类的 `private` 成员，需通过 protected/public 方法 |
| **多态（Polymorphism）** | 封装保证了对象状态的安全，多态在此基础上实现灵活调用 |
| **抽象（Abstraction）** | 封装是实现抽象的基础（隐藏细节，暴露接口） |

---

### 🏷️ Obsidian 标签建议
```markdown
#Java #OOP #封装 #Encapsulation #数据隐藏 #访问控制 #面向对象特性
```

---

