帮助你系统化地掌握面向对象编程中最重要的设计原则之一：**封装（Encapsulation）**。

---

# 📘 Java 12 封装学习指南  
> **目标：理解封装的本质，掌握 getter/setter 的使用，学会用 Lombok 提升开发效率**

---

## 📌 学习路径概览

| 序号 | 主题 | 核心目标 |
|------|------|----------|
| 01 | 对象的封装 | 理解“封装”的概念和意义 |
| 02 | 成员变量与成员方法 | 区分字段和方法，理解访问控制 |
| 03 | getter 与 setter 方法 | 掌握私有字段的读写方式 |
| 04 | JavaBean 规范 | 了解工业标准类设计规范 |
| 05 | Lombok | 使用注解简化代码，提升编码效率 |

> ✅ **建议学习顺序**：严格按此顺序进行，每一步都为基础！

---

## 🔍 每章预习与学习指南

---

### 📘 [[01-对象的封装]]

#### ✅ 预习问题
- 什么是“封装”？它和“隐藏”有什么区别？
- 为什么需要封装？
- 不封装会带来什么问题？

#### 🧠 核心知识点
- **封装 = 数据 + 行为 + 访问控制**
- 目标：**保护数据不被随意修改，只通过合法接口操作**
- 原则：**“高内聚，低耦合”**

> 🌐 类比：
> - 一个银行账户 → 余额是私有的，只能通过存款/取款接口操作
> - 不能直接 `account.balance = -1000`！

```java
class BankAccount {
    private double balance; // 私有字段，外部无法直接访问

    public void deposit(double amount) {
        if (amount > 0) {
            balance += amount;
        }
    }

    public void withdraw(double amount) {
        if (amount <= balance) {
            balance -= amount;
        }
    }
}
```

#### 💡 学习建议
- 写一个 `Temperature` 类，温度值不能为负数
- 思考：如果不用封装，别人怎么破坏你的逻辑？

---

### 📘 [[02-成员变量与成员方法]]

#### ✅ 预习问题
- 成员变量和局部变量有什么区别？
- 成员方法的作用是什么？
- 如何定义一个安全的类？

#### 🧠 核心知识点
| 类型 | 特点 | 示例 |
|------|------|------|
| **成员变量（字段）** | 属于类的实例，每个对象一份 | `private String name;` |
| **成员方法** | 定义对象的行为 | `public void speak()` |
| **局部变量** | 在方法内部定义，作用域有限 | `int i = 0;` |

```java
class Student {
    private String name;        // 成员变量
    private int age;

    public void study() {       // 成员方法
        System.out.println(name + " 正在学习");
    }

    public void setAge(int age) { // 通过方法控制访问
        if (age >= 0 && age <= 150) {
            this.age = age;
        }
    }
}
```

#### 💡 学习建议
- 创建一个 `Car` 类，包含速度、品牌等字段
- 定义 `start()`、`stop()` 方法
- 尝试在主方法中直接修改字段 → 编译错误！

---

### 📘 [[03-getter 与 setter 方法]]

#### ✅ 预习问题
- 为什么要写 `getter` 和 `setter`？
- `getter` 和 `setter` 的命名规则是什么？
- 能否省略它们？

#### 🧠 核心知识点
- **`getter`**：获取字段值（`getXXX()`）
- **`setter`**：设置字段值（`setXXX()`）
- **遵循 JavaBean 规范**，便于框架使用（如 Spring、Jackson）

```java
class Person {
    private String name;
    private int age;

    // Getter
    public String getName() {
        return name;
    }

    // Setter
    public void setName(String name) {
        this.name = name;
    }

    public int getAge() {
        return age;
    }

    public void setAge(int age) {
        if (age >= 0 && age <= 150) {
            this.age = age;
        } else {
            throw new IllegalArgumentException("年龄必须在 0~150 之间");
        }
    }
}
```

> ✅ 使用：
```java
Person p = new Person();
p.setName("小明");
p.setAge(18);
System.out.println(p.getName()); // 输出：小明
```

#### 💡 学习建议
- 写一个 `Product` 类，价格不能为负
- 用 `setter` 实现业务校验（如：库存不能超过 1000）
- 练习生成所有 `getter/setter`（IDE 自动生成功能）

---

### 📘 [[04-JavaBean 规范]]

#### ✅ 预习问题
- 什么是 JavaBean？
- 它和普通类有什么区别？
- 为什么很多框架要求类是 JavaBean？

#### 🧠 核心知识点
**JavaBean 是一种符合特定规范的 Java 类**，主要用于组件化开发和框架集成。

> ✅ **JavaBean 规范要求**：
1. 公共无参构造器（`public ClassName()`）
2. 所有字段为 `private`
3. 提供 `getter` 和 `setter` 方法
4. 实现 `Serializable` 接口（可选，用于序列化）

```java
public class User implements Serializable {
    private String username;
    private String password;

    public User() {}

    public String getUsername() { return username; }
    public void setUsername(String username) { this.username = username; }

    public String getPassword() { return password; }
    public void setPassword(String password) { this.password = password; }
}
```

> ✅ 用途：
- Spring MVC 中接收表单数据
- Jackson / Gson 序列化 JSON
- Hibernate / JPA 映射数据库实体

#### 💡 学习建议
- 创建一个 `Order` 类，符合 JavaBean 规范
- 使用 IntelliJ IDEA 自动生成 `getter/setter` 和 `toString()`
- 查看 Spring Boot 中如何使用此类

---

### 📘[[05-Lombok]]

#### ✅ 预习问题
- Lombok 是什么？它能做什么？
- 为什么需要它？
- 使用 Lombok 是否会影响性能？

#### 🧠 核心知识点
**Lombok 是一个 Java 注解库**，通过编译时注解自动生成代码，减少样板代码。

> ✅ 常用注解：
| 注解 | 功能 |
|------|------|
| `@Data` | 自动生成 `getter`、`setter`、`toString`、`equals`、`hashCode` |
| `@NoArgsConstructor` | 生成无参构造器 |
| `@AllArgsConstructor` | 生成全参构造器 |
| `@ToString` | 生成 `toString()` |
| `@EqualsAndHashCode` | 生成 `equals()` 和 `hashCode()` |

```java
import lombok.Data;

@Data
@NoArgsConstructor
@AllArgsConstructor
public class Student {
    private String name;
    private int age;
}
```

> ✅ 效果等价于手动写 10+ 行代码！

#### 💡 学习建议
1. 添加 Lombok 依赖（Maven / Gradle）
2. 在项目中启用 Lombok 支持（IDE 配置）
3. 用 `@Data` 替换手动写的 `getter/setter`
4. 注意：**生产环境仍需理解底层实现**

---

## ✅ 总结口诀
> **“封装保安全，字段要私有；  
> getter 读数据，setter 控权限；  
> JavaBean 有规范，框架才认得；  
> Lombok 减重复，效率翻倍高。”**

---

